version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_IMAGE:-$DOCKER_USERNAME/$GITHUB_REPOSITORY_NAME:latest}
    container_name: my-app-container
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

